// The editor's fixed text/highlight palette. Kept small and explicit (not a
// free colour picker) and shared by everything that has to agree on the exact
// values: the toolbar (src/components/admin/rich-text-editor.tsx) and the Word
// importer, which normalises Word's own colours onto these.
export const TEXT_RED = "#dc2626";
export const TEXT_BLUE = "#2563eb";
export const HIGHLIGHT_PINK = "#FF00FF";
